#include <bits/stdc++.h>

using namespace std;

#define ll long long
vector<ll>arr,prefix;
ll y = (pow(10,9) + 7);

void solve(){
    ll n,m,d;
    cin >> n >> m >> d;
    vector<ll> v;
    for(ll i=0;i<m;i++){
        int a;cin >> a;
        v.push_back(a);
    }
}

int main(){
    int t;
    cin>>t;
    while(t--){
        solve();
    }
}